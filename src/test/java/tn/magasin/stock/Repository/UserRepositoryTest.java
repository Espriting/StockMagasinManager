package tn.magasin.stock.Repository;

import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import tn.magasin.stock.Service.UserService;
import tn.magasin.stock.entity.User;

import static org.assertj.core.api.AssertionsForClassTypes.assertThat;

class UserRepositoryTest {
   /* @Autowired
    private UserRepository underTest;
    @Autowired
    private UserService userService;
    @Test
    void selectExistsRole() {
        //what I give
        String email="monam.bengouta@esprit.tn";
        User user=new User();
        user.setEmail(email);

        userService.addUser(user);
        //when
        Boolean expected=underTest.selectExistsMail(email);
        assertThat(expected).isTrue();




        //then

    } */
}